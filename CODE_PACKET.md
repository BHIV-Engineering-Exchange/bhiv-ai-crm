# CODE PACKET

## Bright Connection & SETU Convergence Deliverables

**Target Platform:** Bright Connection Tenant (`tenant_bright_connection`)
**Integration Type:** Tally -> Connector -> ARTHA -> SETU/NIYANTRAN
**Owner:** Pritesh
**Deliverable Status:** LIVE / READY

---

### Core Artifacts Included in this Packet

#### 1. Connector & Canonical Pipeline
The translation layer mapping Bright Connection API schemas into Master Data Unit (MDU) formats with guaranteed provenance.
- **File:** `backend/setu/bright_connection_connector.py`
- **Responsibility:** Ingests catalogs, orders, and field visits. Enforces `source_context` construction mapping Tally godowns/stores directly into traceable entities.

#### 2. Live Runtime & Routing Integration
The backend infrastructure configured to host the live endpoints and integrate with Tally Godowns and Mitra account queries without schema or port collisions.
- **Routing Module:** `backend/setu/routes.py`
- **FastAPI Core:** `backend/api_app.py`
- **Startup Script (Fixed):** `backend/start_server.py`

#### 3. Live Proof Harness
The true HTTP client demonstrating end-to-end integration over the network (replacing local mocked TestClients).
- **File:** `e2e_real_bright_connection_proof.py`
- **Capabilities Proven:**
  - Field workflow ingestion with location & photo evidence.
  - Automatic Dealer/Admin notification extraction.
  - Provenance envelope retention (Source entity, timestamp, location, company).
  - Bounded, tenant-scoped Mitra querying based on the extracted `store_id`.

#### 4. Architecture Fixes Applied
- **Port Collision Resolved:** `backend/api_app.py` explicitly shifted to `port=8001` to safely co-reside with the Express backend on `port=8000`.
- **Startup Chain Secured:** Created `start_server.py` to satisfy `Procfile` and deployment configurations without silent failures.
- **Cross-Repo References Eradicated:** All review documentation (`SUBMISSION_REVIEW_PACKET.md`) now points strictly to intra-repository files, removing local dependencies on `D:/Internship Task/`.

---
*Generated by the AI CRM Code Integration Pipeline.*

---

## T-GOV-002 Code Packet

### Scope

Phase 2 security hardening for the Python CRM/Logistics backend. SETU ownership and contracts are unchanged.

### Changed Files

- `backend/phase2_security.py`: RSA key loading, SQLite replay claims, and SHA-256 chained audit log.
- `backend/auth_system.py`: RS256 signing and verification, token `jti`, request replay claims, and auth audit events.
- `backend/security_config.py`: RS256 configuration exports and production key fields.
- `backend/requirements.txt`: exact dependency pins.
- `backend/tests/test_phase2_security.py`: signature, replay, and tamper tests.

### Proof Commands

```text
cd backend
python -m pytest -q tests/test_phase2_security.py
```

Expected result: 4 passed.

### Runtime Configuration

Production requires `JWT_PRIVATE_KEY` and `JWT_PUBLIC_KEY`. New requests should include a unique `X-Request-ID`; duplicate request IDs for the same token are rejected with HTTP 401. Audit and replay SQLite files are written below `SECURITY_DATA_DIR` or `backend/data`.
